# ─── API URL ─────────────────────────────────────────────────────────────────
# URL of the backend API gateway.
# Default for local development against apps/api-java (server.port defaults to 8080):
VITE_API_URL="http://localhost:8080/api/v1"
