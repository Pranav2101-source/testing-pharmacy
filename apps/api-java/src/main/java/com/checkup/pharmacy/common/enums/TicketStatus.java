package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `TicketStatus` enum. */
public enum TicketStatus {
    OPEN,
    ASSIGNED,
    IN_PROGRESS,
    PENDING_USER,
    RESOLVED,
    CLOSED
}
