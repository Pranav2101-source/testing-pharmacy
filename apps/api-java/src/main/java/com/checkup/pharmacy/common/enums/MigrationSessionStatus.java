package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `MigrationSessionStatus` enum. */
public enum MigrationSessionStatus {
    IN_PROGRESS,
    COMPLETED,
    ROLLED_BACK,
    FAILED
}
