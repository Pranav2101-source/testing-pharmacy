package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `MigrationEntityType` enum. */
public enum MigrationEntityType {
    INVENTORY,
    SUPPLIERS,
    CUSTOMERS,
    DOCTORS,
    MEDICINE
}
