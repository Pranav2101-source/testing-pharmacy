package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `AuditModule` enum. */
public enum AuditModule {
    AUTH,
    TENANTS,
    SUBSCRIPTIONS,
    SUPPORT,
    SETTINGS,
    SYSTEM,
    ANALYTICS,
    AUDIT,
    BILLING,
    INVENTORY
}
