package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `SupplierReturnReason` enum. */
public enum SupplierReturnReason {
    DAMAGED,
    NEAR_EXPIRY,
    EXPIRED,
    WRONG_PRODUCT,
    QUALITY_ISSUE,
    SHORT_SUPPLY,
    OTHER
}
