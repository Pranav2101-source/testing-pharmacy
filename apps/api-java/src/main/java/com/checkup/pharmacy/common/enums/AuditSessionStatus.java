package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `AuditSessionStatus` enum. */
public enum AuditSessionStatus {
    DRAFT,
    IN_PROGRESS,
    COMPLETED,
    APPROVED,
    CANCELLED
}
