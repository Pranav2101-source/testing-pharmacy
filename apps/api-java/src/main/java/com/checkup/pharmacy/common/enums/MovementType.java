package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `MovementType` enum. */
public enum MovementType {
    SALE,
    RETURN,
    PURCHASE,
    ADJUSTMENT,
    OPENING,
    DAMAGE,
    EXPIRY_REMOVAL
}
