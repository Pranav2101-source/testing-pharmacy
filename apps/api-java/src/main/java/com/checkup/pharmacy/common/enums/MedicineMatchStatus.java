package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `MedicineMatchStatus` enum — see {@code PharmacyMedicine}. */
public enum MedicineMatchStatus {
    PENDING,
    SUGGESTED,
    LINKED,
    KEPT_LOCAL
}
