package com.checkup.pharmacy.common.enums;

/** Mirrors the Prisma `UploadType` enum. */
public enum UploadType {
    PRESCRIPTION,
    INVOICE_PDF,
    PURCHASE_ORDER_PDF,
    GRN_PDF,
    LOGO,
    REPORT,
    PHARMACY_DOCUMENT
}
